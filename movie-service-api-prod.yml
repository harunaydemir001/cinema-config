server:
  port: 8081

eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8071/eureka/
    fetch-registry: true
    register-with-eureka: true