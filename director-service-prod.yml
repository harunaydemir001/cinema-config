server:
  port: 8084

eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8071/eureka/
    fetch-registry: true
    register-with-eureka: true

logging:
  level:
    root: info
